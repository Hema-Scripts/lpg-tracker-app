<?xml version="1.0" encoding="utf-8"?>
<!-- Shown briefly while the Flutter engine initializes. Kept as a plain
     solid color matching the app's brand orange rather than the default
     white/black template, so the launch doesn't flash white. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_color"/>
</layer-list>
